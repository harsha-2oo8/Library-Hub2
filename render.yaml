services:
  - type: web
    name: library-api
    env: node
    plan: free
    buildCommand: pnpm install && pnpm --filter @workspace/api-server run build
    startCommand: pnpm --filter @workspace/api-server run start
    healthCheckPath: /api/healthz
    envVars:
      - key: PORT
        value: 8080
      - key: NODE_VERSION
        value: "24"
      - key: DATABASE_URL
        value: postgresql://postgres:MS21vryt3ts2dHl0@db.hzbiynbwlapitniiwosl.supabase.co:5432/postgres?sslmode=require
      - key: SESSION_SECRET
        generateValue: true
