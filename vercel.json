{
  "buildCommand": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/library-app run build",
  "outputDirectory": "artifacts/library-app/dist/public",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.ts" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
